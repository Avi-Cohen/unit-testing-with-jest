const {
    capitalize,
    getAlpha2Code,
    countryExtractor,
    countryListLookup,
    getResponse
  } = require("../language_spoken")
  test("convert array of country data objects to array of countries", ()=> {})
  test("convert array of country data objects to array of countries 2", ()=> {})